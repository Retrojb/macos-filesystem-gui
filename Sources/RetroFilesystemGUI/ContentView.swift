import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Text("Retro Filesystem GUI")
                .font(.largeTitle)
                .padding()
        }
        .frame(maxWidth: .infinity, maxHeight: .infinity)
    }
}
